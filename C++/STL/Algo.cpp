#include <iostream>
#include <algorithm>
using namespace std;

// bool compare(int a, int b)
// {
//     return a > b;
// }

int main()
{

    //FIND

    // int a[] = {3, 4, 5, 8, 9};
    // int n = sizeof(a) / sizeof(a[0]);

    // auto it = find(a, a + n, 2);

    // if ((it - a) == n)
    // {
    //     cout << "Not Found!" << endl;
    // }
    // else
    // {
    //     cout << "Found at index " << it - a << endl;
    // }

    //////////////////////////////////////////////////////////////////////

    //Binary Search

    //     int a[] = {3, 4, 5, 8, 9};
    //     int n = sizeof(a) / sizeof(a[0]);

    //     bool present = binary_search(a, a + n, 1);

    //     if (present)
    //     {
    //         cout << "Found!" << endl;
    //     }
    //     else
    //     {
    //         cout << "Not Found! " << endl;
    //     }

    ///////////////////////////////////////////////////////////////////////////

    //Sort

    // int a[] = {3, 4, 5, 8, 9};
    // int n = sizeof(a) / sizeof(a[0]);

    // sort(a, a + n, compare);

    // for (int i = 0; i < n; i++)
    // {
    //     cout << a[i] << " ";
    // }

    ////////////////////////////////////////////////////////////////////////////

    //ROTATE
    // int a[] = {3, 4, 5, 8, 9};
    // int n = sizeof(a) / sizeof(a[0]);

    // rotate(a, a + 4, a + n);
    // for (int i = 0; i < n; i++)
    // {
    //     cout << a[i] << " ";
    // }

    //////////////////////////////////////////////////////////////////////////////////

    //NEXT_PERMUTATION
    // int a[] = {3, 1, 4};
    // int n = sizeof(a) / sizeof(a[0]);

    // next_permutation(a, a + n);
    // next_permutation(a, a + n);
    // for (int i = 0; i < n; i++)
    // {
    //     cout << a[i] << " ";
    // }
}
